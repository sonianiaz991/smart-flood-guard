services:
  - type: web
    name: smart-flood-guard
    env: python
    plan: free
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    runtime: python
    pythonVersion: 3.11.9